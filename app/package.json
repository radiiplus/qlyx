{
  "name": "reader",
  "version": "1.0.0",
  "private": true,
  "description": "Concurrent filesystem and process engine for automation",
  "type": "module",
  "exports": "./src/tool.ts",
  "scripts": {
    "start": "node src/tool.ts",
    "server": "node src/server.ts",
    "test": "node --test test/test.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22.18"
  },
  "dependencies": {
    "@fastify/websocket": "^11.3.0",
    "fastify": "^5.12.1"
  }
}
